spring:
  application:
    name: todo-api-gateway
server:
  port: 8765
eureka:
  instance:
    hostname: todo-service-registry
    prefer-ip-address: true
  client:
    service-url:
      default-zone: ${EUREKA_SERVER:http://localhost:8761/eureka}